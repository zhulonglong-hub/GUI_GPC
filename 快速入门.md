# GPC Dataset Manager - 快速入门指南

## 安装和配置

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置数据集路径

编辑 `config.py` 文件的第18行:

```python
DATASET_ROOT = Path(r"D:\GeoPCDataset_V1.0")  # 改为你的数据集路径
```

### 3. 启动程序

```bash
python main.py
```

---

## 功能快速使用

### 📷 浏览样本

1. 选择搜索方式 (task_id / image_id / name / phrase)
2. 输入关键词,点击"搜索"
3. 点击列表项查看图像和标注详情

**提示**: 使用 image_id 前缀匹配可快速定位图像

---

### ➕ 新增样本

1. 点击"选择图像"选择图像文件
2. 填写类别名称 (name) - **必填**
3. 填写属性 (attributes) - 多个用逗号分隔
4. 输入多边形数据或使用矩形框:
   - **多边形格式**: `[[[[x1,y1], [x2,y2], [x3,y3], ...]]]`
   - **矩形框格式**: `x_min, y_min, x_max, y_max` (可转换为多边形)
5. 选择数据集划分 (train/val/test)
6. 点击"提交新增"

**示例多边形**:
```json
[[[[10, 10], [100, 10], [100, 100], [10, 100]]]]
```

**示例矩形框**:
```
50, 50, 200, 150
```

---

### 🗑️ 删除样本

1. 输入 task_id 或 image_id
2. 点击"搜索"
3. 勾选要删除的条目 (支持多选)
4. (可选) 勾选"同时删除图像文件"
5. 点击"删除选中条目"并确认

**注意**: 
- 删除会备份到 `recycle/` 目录
- 一图多膜场景会显示所有关联标注

---

### ✏️ 编辑字段

1. 输入 task_id
2. 点击"加载"
3. 修改需要变更的字段 (其他字段留空)
4. 点击"保存修改"并确认

**可编辑字段**:
- phrase (短语)
- name (类别名)
- attributes (属性列表)

---

### 📊 查看统计

- 自动显示数据集概览
- TOP-20 类别分布
- 最近20条操作记录
- 点击"刷新"更新数据

---

## 常见问题

### Q: 启动时提示"数据集结构不完整"?

**A**: 检查 `config.py` 中的 `DATASET_ROOT` 路径是否正确,确保目录包含:
- `annotations/` 目录
- `images/` 目录
- 必要的JSON文件

### Q: 搜索没有结果?

**A**: 
1. 尝试点击菜单"文件 → 刷新索引"
2. 检查搜索关键词拼写
3. 尝试切换搜索方式

### Q: 图像显示不出来?

**A**: 
1. 确认 `images/` 目录中有对应文件
2. 检查图像格式是否支持 (.jpg/.png/.tif)
3. 查看状态栏的错误提示

### Q: 删除后能恢复吗?

**A**: 可以! 删除的记录备份在 `recycle/` 目录中,可手动恢复

---

## 快捷操作

- **搜索框回车** = 执行搜索
- **全选/取消全选** = 批量选择删除条目
- **预览按钮** = 新增前预览生成的记录

---

## 数据安全

✅ 所有写操作都会:
1. 先备份原文件
2. 使用临时文件写入
3. 原子替换保证安全
4. 记录操作日志

✅ 删除操作会:
1. 备份到 `recycle/` 目录
2. 二次确认提示
3. 可选择是否删除图像文件

---

## 技术支持

查看完整文档: `README.md`  
查看实施总结: `项目实施总结.md`  
操作日志位置: `logs/operations.jsonl`

---

## 版本信息

当前版本: **v1.0**  
发布日期: 2024-06-02

**已实现功能**:
- ✅ 浏览与预览
- ✅ 新增样本
- ✅ 删除样本
- ✅ 编辑字段
- ✅ 统计分析

**计划功能** (v2.0):
- ⏳ 批量编辑
- ⏳ 可视化多边形编辑
- ⏳ 回收站管理
